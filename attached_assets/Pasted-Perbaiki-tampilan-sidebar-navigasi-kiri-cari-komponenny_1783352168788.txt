Perbaiki tampilan sidebar navigasi kiri (cari komponennya di kode — mungkin ada di components/AppShell.tsx jika sudah dibuat dari prompt sebelumnya, atau masih berupa function CatalogSidebar/sejenisnya di dalam salah satu file page seperti CatalogPage.tsx. Temukan lokasi aktualnya di kode sebelum mengubah apapun).
1. Perbaiki ikon yang terlihat sama. Saat ini menu "Dashboard" dan "Katalog Kelas" memakai ikon yang secara visual terlihat identik (sama-sama kotak grid 2x2), padahal seharusnya beda. Pastikan "Dashboard" memakai ikon LayoutDashboard dari lucide-react (bentuknya seperti panel dengan 1 kotak besar di kiri + 2 kotak kecil di kanan, BUKAN grid 2x2 rata), dan "Katalog Kelas" tetap memakai LayoutGrid (grid 2x2 rata). Kalau setelah dicek keduanya ternyata sudah benar secara kode tapi tetap terlihat mirip di ukuran kecil (16px), perbesar sedikit ukuran ikon jadi h-[18px] w-[18px] untuk kejelasan.
2. Tambahkan label kecil di atas menu navigasi. Sebelum daftar menu (Dashboard, Katalog Kelas, Kelas Saya), tambahkan teks label kecil: <p className="px-3 text-[11px] font-semibold uppercase tracking-wider text-muted-foreground/60 mb-2">Menu Utama</p>.
3. Isi ruang kosong di bawah dengan card profil user, dipin ke paling bawah sidebar.
Ubah struktur container sidebar (elemen <aside>) agar memakai flex flex-col dengan tinggi penuh (h-full atau sudah otomatis dari fixed inset-y-0), lalu setelah <nav> yang berisi menu, tambahkan <div className="flex-1" /> sebagai spacer yang mendorong elemen berikutnya ke bawah, baru setelah itu card profil.
Card profil (letakkan di paling bawah, di atas border-top sebagai pemisah): <div className="border-t border-border p-4">, isinya flex items-center gap-3: avatar bulat berisi inisial (h-9 w-9 rounded-full bg-primary/10 text-primary flex items-center justify-center text-sm font-semibold — inisial dari user?.nickname?.[0]?.toUpperCase() ?? user?.name?.[0]?.toUpperCase() ?? 'U'), di sampingnya <div className="flex-1 min-w-0"> berisi nama (text-sm font-semibold text-foreground truncate, tampilkan user?.nickname ?? user?.name) dan email di bawahnya kecil (text-xs text-muted-foreground truncate, tampilkan user?.email). Di ujung kanan, tombol icon kecil logout: <button onClick={logout}> (ambil logout dari useAuth()) berisi ikon LogOut dari lucide-react ukuran h-4 w-4, dengan className="text-muted-foreground hover:text-destructive transition-colors p-1.5 rounded-md hover:bg-muted".
Pastikan komponen sidebar ini menerima/mengakses user dan logout dari useAuth() (import useAuth dari @/context/AuthContext jika belum ada).
4. Verifikasi:

pnpm run typecheck, harus 0 error.
Screenshot sidebar setelah perubahan — pastikan: ikon Dashboard & Katalog Kelas terlihat beda, ada label "Menu Utama" kecil di atas menu, dan card profil (avatar+nama+tombol logout) nempel di paling bawah sidebar, bukan di tengah atau mengambang.
Klik tombol logout di card profil itu — pastikan berhasil logout dan diarahkan balik ke halaman yang sesuai (biasanya landing page atau login).
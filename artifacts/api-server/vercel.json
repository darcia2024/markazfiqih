{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/api-server run build && mkdir -p public && echo 'API server - lihat /api untuk endpoint' > public/index.html",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}

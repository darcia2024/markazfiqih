{
  "version": 2,
  "buildCommand": "mkdir -p public && echo 'API server - lihat /api untuk endpoint' > public/index.html",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}

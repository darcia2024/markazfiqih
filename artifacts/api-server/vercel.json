{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/api-server run build && mkdir -p public && echo 'API server' > public/index.html",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
